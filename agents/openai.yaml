interface:
  display_name: "NeuBE-Structural-Rebuild"
  short_description: "Build domain packs and publish traceable structure models"
  default_prompt: "Use $general-structure-reconstruction to create a domain pack and run a traceable structural reconstruction project."
